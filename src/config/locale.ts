/**
 * The locale the suite pins (playwright.config.ts `use.locale`). Page objects
 * format dates with it so assertions match what the site renders — change the
 * pinned locale here and in the config together, never inline.
 */
export const LOCALE = 'en-US';
