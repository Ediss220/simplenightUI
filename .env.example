# Test environment. Known presets live in src/config/env.ts ("staging").
SN_ENV=staging

# Alternatively, point the suite at any deployment directly (overrides SN_ENV):
# SN_BASE_URL=https://wl.stg.simplenight.com
